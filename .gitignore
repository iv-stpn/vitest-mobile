# Dependencies
node_modules/

# OS
.DS_Store

# Logs
*.log
npm-debug.log*

# Env
.env
.env.local
.env.*.local

*.local.json

# Pack artifacts
*.tgz

# Expo
.expo/

# Build artifacts
test-app/ios/
test-app/android/
test-app/build/

# vitest-mobile (auto-generated)
# Note: .vitest-mobile/ is NOT gitignored here — Metro's watchman must be able
# to crawl it to find the generated entry files. The inner .vitest-mobile/.gitignore
# (with *) already prevents the generated files from being committed.
cmdline-tools.zip
